var game = {
    points: 0,
    atoms: 0,
    atomResets: 0,
}

window.setInterval(function() {
    let res = {    
        ["points"]: game.points
    }
    for (i = 0; res.length; i++ ) {
        for (let j = 0; j < $(Object.keys(res)[j], 2).length; j++) {
            $(Object.keys)
        }
    }
}, 100)

$("p", 4)